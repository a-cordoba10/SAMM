import { Meteor } from 'meteor/meteor';
import '../imports/api/SavedSongs.js';
import '../imports/api/SessionSongs.js';
//El proyecto esta vacío
Meteor.startup(() => {
  // code to run on server at startup
});
